# A phone-sized screen for every Robolectric test: the default 320x470dp
# viewport clips the lower half of HomeScreen/SettingsScreen, which makes
# Compose click injection miss nodes that are laid out below the fold.
qualifiers=w411dp-h891dp
